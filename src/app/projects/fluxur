"use client";

import Link from "next/link";
import Image from "next/image";
import { ArrowLeft, Github, Globe } from "lucide-react";

function XIcon({ size = 18 }: { size?: number }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
      <path d="M4 4l11.733 16h4.267l-11.733 -16z" />
      <path d="M4 20l6.768 -6.768m2.46 -2.46l6.772 -6.772" />
    </svg>
  );
}

export default function FluxurPage() {
  const techTags = ["solana", "rust", "typescript", "javascript", "css"];

  const stackItems = [
    { name: "Rust", desc: "Core smart contracts" },
    { name: "Solana", desc: "Blockchain layer" },
    { name: "TypeScript", desc: "Frontend + tooling" },
    { name: "JavaScript", desc: "Web app logic" },
    { name: "CSS", desc: "UI styling" },
  ];

  return (
    <main className="min-h-screen relative z-10">
      <div className="max-w-2xl mx-auto px-6 py-16 md:py-24">
        <Link href="/" className="inline-flex items-center gap-2 text-xs text-zinc-600 hover:text-emerald-400 transition-colors mb-12 group">
          <ArrowLeft size={14} className="group-hover:-translate-x-1 transition-transform" />
          <span className="tracking-wide">back</span>
        </Link>

        <header className="mb-16 animate-fade-in">
          <div className="flex items-center gap-4 mb-4">
            <div className="w-16 h-16 rounded-xl overflow-hidden bg-zinc-900 flex-shrink-0">
              <Image src="/fluxur-logo.png" alt="Fluxur logo" width={64} height={64} className="w-full h-full object-cover" />
            </div>
            <div>
              <h1 className="text-4xl md:text-5xl font-display tracking-tight text-zinc-100">Fluxur</h1>
              <p className="text-sm text-zinc-600 mt-1">2025 - present</p>
            </div>
          </div>

          <div className="flex items-center gap-3 mb-6">
            <a
              href="https://x.com/FluxurFun"
              target="_blank"
              rel="noopener noreferrer"
              className="p-2 text-zinc-600 hover:text-emerald-400 transition-colors rounded-lg border border-zinc-800 hover:border-zinc-700 bg-zinc-900/50"
              aria-label="X (Twitter)"
            >
              <XIcon size={16} />
            </a>
            <a
              href="https://github.com/ahsaxyz/fluxur"
              target="_blank"
              rel="noopener noreferrer"
              className="p-2 text-zinc-600 hover:text-emerald-400 transition-colors rounded-lg border border-zinc-800 hover:border-zinc-700 bg-zinc-900/50"
              aria-label="GitHub"
            >
              <Github size={16} strokeWidth={1.5} />
            </a>
            <a
              href="http://fluxur.fun/"
              target="_blank"
              rel="noopener noreferrer"
              className="p-2 text-zinc-600 hover:text-emerald-400 transition-colors rounded-lg border border-zinc-800 hover:border-zinc-700 bg-zinc-900/50"
              aria-label="Website"
            >
              <Globe size={16} strokeWidth={1.5} />
            </a>
          </div>

          <div className="flex flex-wrap gap-2 mb-8">
            {techTags.map((tech) => (
              <span key={tech} className="text-[9px] uppercase tracking-[0.15em] text-zinc-600 px-2 py-1 rounded bg-zinc-900/80 border border-zinc-800/50">{tech}</span>
            ))}
          </div>

          <p className="text-zinc-400 leading-relaxed text-sm md:text-[15px]">
            Protocol for provable commitment in on-chain creator ecosystems.
          </p>
        </header>

        <section className="mb-16 animate-fade-in-delay-1">
          <div className="flex items-center gap-4 mb-6">
            <span className="text-[10px] uppercase tracking-[0.25em] text-zinc-600 font-medium">overview</span>
            <div className="flex-1 h-px bg-gradient-to-r from-zinc-800 to-transparent" />
          </div>
          <div className="text-zinc-500 leading-[1.8] text-sm space-y-4">
            <p>Fluxur is a protocol built to make commitment provable on-chain.</p>

            <p>In creator-driven ecosystems, promises are easy to make but difficult to verify. Fluxur introduces time-based, on-chain commitment infrastructure that allows developers and creators to demonstrate long-term alignment with their communities through irreversible smart contract actions.</p>

            <p>Instead of relying on trust, social signals, or off-chain agreements, Fluxur enables transparent commitment through programmable locks, public verification, and time-based unlock mechanisms deployed directly on Solana.</p>

            <p>The protocol allows creators to lock pump.fun creator fees and other assets into dedicated vaults with predefined unlock schedules. These locks are fully on-chain, non-custodial, and publicly viewable, creating a verifiable record of commitment that anyone can audit.</p>

            <p>Fluxur also introduces vanity FLXR-ending token addresses and integrated launch tooling, allowing new and existing tokens to connect directly into the commitment layer. By combining token creation, fee locking, and public dashboards, Fluxur establishes a new primitive for trustless creator alignment.</p>

            <p>The long-term vision for Fluxur is to become foundational infrastructure for on-chain reputation and commitment, enabling governance, staking, and incentive systems that reward long-term builders and aligned communities.</p>

            <p>Built on Solana using Rust and Anchor, Fluxur is designed for speed, transparency, and fully verifiable execution. Every core action within the protocol is recorded on-chain and accessible to the public, reinforcing the principle that commitment should be demonstrated through code, not words.</p>

            <p>Fluxur represents a shift toward provable alignment in decentralized ecosystems, where builders can show conviction, not just claim it.</p>
          </div>
        </section>

        <section className="mb-16 animate-fade-in-delay-2">
          <div className="flex items-center gap-4 mb-6">
            <span className="text-[10px] uppercase tracking-[0.25em] text-zinc-600 font-medium">stack</span>
            <div className="flex-1 h-px bg-gradient-to-r from-zinc-800 to-transparent" />
          </div>
          <div className="grid grid-cols-2 md:grid-cols-3 gap-3">
            {stackItems.map((item) => (
              <div key={item.name} className="p-4 rounded-lg border border-zinc-900 bg-zinc-950/50 hover:border-zinc-800 transition-colors">
                <p className="text-zinc-300 text-sm font-medium mb-1">{item.name}</p>
                <p className="text-zinc-700 text-[10px] uppercase tracking-wider">{item.desc}</p>
              </div>
            ))}
          </div>
        </section>

        <footer className="pt-8 border-t border-zinc-900">
          <Link href="/" className="inline-flex items-center gap-2 text-xs text-zinc-600 hover:text-emerald-400 transition-colors group">
            <ArrowLeft size={14} className="group-hover:-translate-x-1 transition-transform" />
            <span className="tracking-wide">back to home</span>
          </Link>
        </footer>
      </div>
    </main>
  );
}
